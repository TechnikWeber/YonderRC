{
  "name": "@yonderrc/desktop",
  "version": "1.13.0",
  "private": true,
  "description": "YonderRC desktop shell (Electron) with native SDL2 controller input and rumble.",
  "main": "dist/main.js",
  "scripts": {
    "compile": "tsc -p tsconfig.json",
    "dev": "npm run compile && cross-env YRC_DEV=1 electron .",
    "copy-renderer": "node scripts/copy-renderer.mjs",
    "build": "npm run compile && npm --prefix ../ground run build && npm run copy-renderer && electron-builder",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {},
  "optionalDependencies": {
    "@kmamal/sdl": "^0.11.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.3"
  },
  "license": "SEE LICENSE IN LICENSE",
  "author": "Philipp Weber"
}
